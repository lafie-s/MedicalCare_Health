# 工程协作规则

适用范围：本仓库及全部子目录。后续开发人员与自动化代理均应遵循。

## 项目定位

- 为 MedicalCareWeb 运维人员建设健康指标监测及维护平台。
- 实现前阅读 `docs/technical-design.md` 和 `docs/project-plan.md`；若需求或方案变化，同步更新文档。
- 已确认健康指标指 MedicalCareWeb 系统运行指标，围绕可用性、接口性能、错误率、资源和依赖状态开展监测及维护。

## 工作目录

- 直接使用当前工程目录及当前分支，不创建、不使用 worktree。
- 修改前检查 Git 状态，保留已有用户修改，不覆盖、不清理与本功能无关的文件。

## 每完成一个功能就上传 Git

“上传 Git”指完成本地 commit **并成功 push 到远程仓库**，仅本地提交不算上传完成。

1. 按可独立验收的功能分步开发，避免多个已完成功能积压到一次提交。
2. 每个功能完成后，执行与改动相匹配的必要检查，确认验收条件满足；更新相关技术文档及计划状态。
3. 检查差异，确保不包含密钥、令牌、生产配置、患者数据或无关改动；只暂存本功能相关文件。
4. 创建清晰的提交，例如 `feat: 增加服务健康概览`、`fix: 修复告警恢复判断`、`docs: 初始化技术方案与甘特图`。
5. 立即推送当前分支至其跟踪分支；首次无跟踪关系时先核实 origin 与目标分支，再执行 `git push -u origin <当前分支>`。
6. 记录验证结果、提交哈希及推送结果。推送失败时，明确报告失败原因，保留本地提交，排除可处理的问题后重试；不得声称已上传成功。
7. 禁止强制推送、重写远程历史、删除远程分支或通过重置丢弃用户修改。出现远程分歧时先检查双方提交，采用保留双方改动的方式解决。

文档初始化、配置变更和缺陷修复同样按上述可独立验收单元提交并推送。

## 功能完成标准

- 约定的正常路径、失败路径及权限控制已实现。
- 与改动有关的测试或人工验证完成，记录真实结果；不能运行的检查注明原因。
- 涉及 UI 时检查加载、空数据、错误、权限不足及数据过期状态。
- 涉及监控时检查无数据、依赖失联、恢复、重复采样及时间窗口边界。
- 涉及维护操作时具备权限校验、审计、超时和重复请求保护。
- 文档与实现一致，Git 提交及远程推送成功。推送受阻时标记“已实现，待上传”，不能标记全部完成。

## 实现约束

- 优先复用 MedicalCareWeb 已有认证、接口及运维基础设施，接入前验证实际能力。
- 使用服务端授权；前端隐藏按钮不能替代权限控制。
- 配置与凭据分离，仅提交脱敏示例；生产凭据从部署环境或密钥管理服务注入。
- 维护能力通过明确的操作白名单和受控接口实现，不提供任意 Shell、SQL 执行入口。
- 指标缺失不得当作正常或零值；展示最近采集时间及数据新鲜度。
