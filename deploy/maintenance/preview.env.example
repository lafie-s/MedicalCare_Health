# Generate a unique secret locally; do not commit this file after filling values.
MAINTENANCE_GATE_KEY=
# Exact current address of the public Nginx container on shengren_default.
TRUSTED_INGRESS_IP=
# Use a reviewed image with auth_request + realip modules, preferably pinned by digest.
MAINTENANCE_NGINX_IMAGE=nginx:stable-alpine
