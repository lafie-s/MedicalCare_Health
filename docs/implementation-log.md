# 实施与验证记录

## 2026-09-07 / F00 可运行后端基础

- 已只读检查 `F:\MedicalCareWeb`：主站为 Next.js / React，chat-service 为 Fastify 5 / TypeScript，已有 `/auth/me`、`/health/live`、`/health/ready`。
- 本平台后端采用相同 Fastify / TypeScript 技术栈，依赖版本与现有服务对齐；不采用初稿中的 .NET 候选方案。前端后续采用 Next.js / React 对齐主站。
- 建立严格 TypeScript 检查、独立测试目录、构建与启动命令、仅监听本机的默认配置、统一错误格式和存活探测。
- 未配置身份和状态存储时就绪探测返回 503，不将空工程标记为运行就绪。
- 没有读取现有工程的生产凭据，没有修改 MedicalCareWeb 文件。

验证：`npm run check` 通过，包含严格类型检查、2 项接口/配置测试及生产构建；`git diff --check` 通过。Git 上传结果随每批交付说明记录。

F00 提交：`7d9fd87`。推送失败：GitHub 443 端口连接失败，保留本地提交。

## 2026-09-07 / F01a 员工身份交换与环境授权

- 新增令牌交换、平台会话、退出、当前身份和授权环境查询接口。
- 复用 MedicalCareWeb `/auth/me` 协议，不自动继承其业务角色；运维授权按员工和环境明确配置。
- 会话最长 15 分钟，SQLite 保存令牌摘要及加密后的上游凭据，创建和撤销与审计事务一致。
- 权限撤销逐请求生效；上游失联拒绝受保护查询，但可本地退出；写请求来源校验及限流已实现。
- 添加单实例配置示例和运行说明。真实员工账号、登录页面及生产存储不在本次完成范围。

验证覆盖：鉴权、环境隔离、权限撤销、上游过期、退出后重放、跨源请求、限流、存储重启与加密、配置错误，以及真实 HTTP 适配器的有效/无效身份、超时、重定向和错误响应。

`npm run check` 验证包括：类型检查、17 项测试、构建及启动构建产物的 HTTP 冒烟检查。冒烟检查使用临时本机端口，并在结束后停止其启动的进程。Git 上传结果见当次交付说明。

## 2026-09-07 / F01b 员工登录与授权环境工作台

- 添加 Next.js / React 前端、中文登录页及授权环境工作台，按项目既有 API 的权限边界显示内容。
- 新增服务端密码登录适配，浏览器只接收本平台 HttpOnly 会话；本次新建的上游 refresh session 立即撤销。
- 统一加载、错误、空环境、身份失效和退出反馈，保持环境选择 URL 状态。
- 创建 DESIGN.md、UX-CONTRACT.md、设计令牌生成与漂移检查、前端严格审计配置。
- 完成 22 项后端测试、前后端构建与类型检查及浏览器流程验证；详情见 `docs/ui-verification.md`。
- 当前真实员工联调仍待实际授权配置；本次没有修改 MedicalCareWeb 工程或账号。

## 2026-09-07 / F02 服务目录

- 实现服务登记、编辑、启停、环境隔离、幂等创建、版本冲突检测及事务审计。
- 新增部署侧目标白名单，页面仅选择当前环境的已批准目标。
- 服务表单使用共享模态对话框，包含未保存修改保护、启停确认和失败保留输入。
- 验证：26 项后端测试、前后端类型检查与构建、进程冒烟检查通过；浏览器验证创建、编辑、启停、原生下拉键盘选择、409 保留输入、Escape 草稿保护及 390px 对话框布局。Premium strict audit 无违规。
- F01b 及更早提交已在 `a894cbd` 推送成功。F02 提交 `e71ffbc` 已成功推送至 `origin/main`。

## 2026-09-07 / F03a HTTP 健康探测

- 实现固定 IP 白名单 HTTP 探测、自动调度、手动触发、角色与环境授权、3 秒超时、并发及重复采样保护。
- SQLite 持久化采样与审计；区分成功、HTTP 异常、连接失败、超时、中断、无数据和过期，按当前配置的 5 分钟有效样本统计成功率。
- 服务页面增加最近响应头耗时、成功率、采集时间及即时探测；支持 30 秒刷新和客户端过期判定。
- 验证：35 项后端测试、前后端类型检查与构建、进程冒烟检查通过。真实本机 HTTP 测试覆盖固定地址/Host、重定向、超时、响应体不下载；SQLite 重启、窗口边界、恢复、幂等和并发验证通过。
- 浏览器使用隔离身份与本机 `/health/live` 验证即时探测、自动恢复、启停及配置失效；路由注入旧时间戳验证过期未知和隐藏旧指标。桌面和 390px 窄屏截图已检查，无横向溢出。详情见 `docs/ui-verification.md`。
- 本次未使用真实员工账号或生产环境，不将本机探测成功记为 M1 完成。Git 上传结果随本次交付报告记录。

F03a 提交 `77d5d80` 已成功推送。

## 2026-09-07 / F04a HTTP 健康概览

- 新增授权环境概览 API，汇总与明细共用查询时间；纯状态模块由 API 和浏览器共享。
- 界面提供四类计数、过期数量、关注清单、原因与负责人，以及键盘可操作的详情链接。
- 空目录、未知或停用覆盖不能判定全正常；已知异常在混合状态中保留。完整告警级别、维护状态及真实联调未记为完成。
- 验证：39 项测试、后端类型检查/构建/进程冒烟、前端令牌检查/生产构建/类型检查通过。浏览器验证空目录、混合状态、键盘链接、错误后清除旧汇总、重试恢复、环境切换和权限撤销；桌面和窄屏截图已检查。首次前端共享模块解析失败，调整 Turbopack 根目录后构建通过。
- 提交及推送结果见当次交付说明。

F04a 提交 `e88b8c1` 已成功推送。

## 2026-09-07 / F05a HTTP 探测趋势

- 实现单服务的 1/6/24 小时查询、固定粒度聚合和服务端授权；最大 96 点，限定当前配置与目标。
- 平均响应头耗时使用独立样本分母，超时/连接失败只计入成功率，中断单列，无数据为 null。界面提供断点图、精确分段表、查询时间、最近采样、错误重试和请求取消。
- 修正 API 错误呈现：优先显示后端错误消息，服务 404 不再一律误报登录未配置。
- `npm run check`：41 项测试、类型检查、构建、进程冒烟通过。`npm run check:web`：令牌检查、生产构建、类型检查通过。
- 浏览器：真实本机趋势接口分桶表、原生下拉打开、1/24 小时切换、空样本、错误重试、403 返回登录、Escape 焦点恢复通过；路由注入稀疏样本检查断点和 96 行表格。桌面/390px 截图已检查，不宣称真实 MedicalCareWeb 联调。
- Git 提交和推送结果见本次交付回复。

F05a 提交 `b474cfc` 已成功推送。

## 2026-09-07 / F06a HTTP 告警规则与事件

- 新增单服务连续失败/成功次数规则、等级、启停与版本冲突保护；默认不启用告警。
- 采样事务内评估、触发去重、自动恢复、人工确认与恢复后关闭；规则/服务/目标变更终止事件并记录原因，事件保留触发时规则快照。
- 46 项测试、后端类型检查/构建/进程冒烟及前端构建/类型检查通过，含重启计数保留、审计失败回滚、乱序/重复/中断、环境隔离和角色边界。初次测试临时数据库清理顺序导致文件占用，调整为先关闭后清理，重跑通过。
- 浏览器使用隔离种子事件调用真实平台 API，验证确认不恢复、已恢复关闭、规则变更确认与终止原因、409 草稿保留、503 清除旧结果及恢复、键盘焦点和窄屏。
- 未配置外部通知，未使用真实员工或生产告警；技术范围与后续门槛见 `docs/http-alerts.md`。本次提交和推送结果见交付回复。

F06a 提交 `8952c1a` 已成功推送。

## 2026-09-08 / F06b 环境告警中心

- 新增环境授权事件查询、状态筛选、20 条分页、全量汇总和越界页码回退；同一读事务内读取计数与列表。
- 概览链接到中心，中心可定位服务、打开既有告警面板，关闭后刷新处理结果。筛选/页码保存在 URL，切换环境重置；原事件行消失时焦点回退到中心。
- 48 项测试、后端类型检查/构建/进程冒烟及前端构建/类型检查通过；覆盖超过 20 条事件、分页不重不漏、筛选独立汇总和跨环境隔离。
- 浏览器验证实际 fixture 确认及返回列表、筛选/页码刷新恢复、原生下拉、空筛选、503 重试与 403 清除权限数据。分页注入 21 条模拟数据，验证下一页、重新加载、上一页。桌面及窄屏截图已检查。
- 当前仍使用隔离测试身份与事件，不代表真实 MedicalCareWeb 联调或通知接入。Git 提交与推送见当次交付。

## F07a 维护窗口记录（2026-09-08）

上一轮 F06b 提交 58d2149 已成功补推至 origin/main。本轮实现登记、取消、分页历史及服务维护标记；仅记录，不改变采集和告警。窗口、审计同事务，创建/取消均有重复保护，服务端校验角色、环境及时间。

验证：npm run check 全部 53 项测试、后端构建和启动 smoke 通过；npm run check:web 令牌检查、生产构建与类型检查通过。隔离浏览器验证登记/取消、空值聚焦、409 保留草稿、Escape 放弃、503 清除旧列表与重试，以及注入窗口到期自动移除标记。1440px/390px 页面检查无横向溢出。未操作真实 MedicalCareWeb 环境。提交和推送结果见当次交付说明。

界面严格审计 0 问题；已将原生日期控件归属写入组件映射与配置。DESIGN lint 0 errors、9 项既有 CSS 令牌引用提示；令牌检查与最终前端类型检查通过。

## F08a 环境操作审计接口（2026-09-08）

维护窗口提交 2ae9168 本轮已成功补推。实现管理员授权环境审计查询，支持类型、时间区间和分页，明确关联已知操作资源并排除无归属事件。隐藏请求标识及业务原因，只返回必要追责字段。不提供审计修改或删除 API。

验证：npm run check 55 项测试全部通过，后端类型检查、构建、启动 smoke 通过；新增环境/角色/参数/只读拒绝测试和操作映射、边界、分页、字段投影测试。未修改前端，本轮不重复浏览器检查。文档明确当前名称非历史快照、登录审计及防篡改范围限制。提交与推送结果见当次交付。

## F08b 管理员审计页面（2026-09-08）

新增环境审计列表、原生操作/时间筛选、20 条分页、URL 恢复、手动刷新和服务定位。管理员以外不渲染，权限失效清除页面。技术文档与甘特图同步更新。

验证：前端生产构建、类型与令牌检查通过，严格 UI 审计 0 问题。浏览器使用隔离 fixture 验证真实查询、空结果、筛选刷新恢复、键盘；注入验证 503 重试、403 清除、21 条分页及只读隐藏。检查桌面和 390px 窄屏及 reduced-motion。后端未修改，沿用 F08a 已通过的 55 项测试结论。Git 结果见当次交付。

## F09a 数据库快照与恢复（2026-09-08）

新增 npm run db:snapshot -- backup/restore 命令。使用 VACUUM INTO 一致性快照、完整性检查和 SHA-256 清单；拒绝覆盖，恢复至新目录并清除旧会话、记录系统审计。同步操作手册和甘特图。

验证：npm run check 56 项测试全部通过，类型、构建、启动 smoke 通过。运行中的 WAL 备份、业务恢复、会话清除与源库不变、损坏校验及目标冲突拒绝已测试。构建后 CLI 使用隔离数据完成 backup/restore，均返回 verified。没有真实环境切换，前端未修改。提交与推送结果见当次交付。
