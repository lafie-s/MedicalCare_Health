# 实施与验证记录

## 2026-09-07 / F00 可运行后端基础

- 已只读检查 `F:\MedicalCareWeb`：主站为 Next.js / React，chat-service 为 Fastify 5 / TypeScript，已有 `/auth/me`、`/health/live`、`/health/ready`。
- 本平台后端采用相同 Fastify / TypeScript 技术栈，依赖版本与现有服务对齐；不采用初稿中的 .NET 候选方案。前端后续采用 Next.js / React 对齐主站。
- 建立严格 TypeScript 检查、独立测试目录、构建与启动命令、仅监听本机的默认配置、统一错误格式和存活探测。
- 未配置身份和状态存储时就绪探测返回 503，不将空工程标记为运行就绪。
- 没有读取现有工程的生产凭据，没有修改 MedicalCareWeb 文件。

验证：`npm run check` 通过，包含严格类型检查、2 项接口/配置测试及生产构建；`git diff --check` 通过。Git 上传结果随每批交付说明记录。

F00 提交：`7d9fd87`。推送失败：GitHub 443 端口连接失败，保留本地提交。

## 2026-09-07 / F01a 员工身份交换与环境授权

- 新增令牌交换、平台会话、退出、当前身份和授权环境查询接口。
- 复用 MedicalCareWeb `/auth/me` 协议，不自动继承其业务角色；运维授权按员工和环境明确配置。
- 会话最长 15 分钟，SQLite 保存令牌摘要及加密后的上游凭据，创建和撤销与审计事务一致。
- 权限撤销逐请求生效；上游失联拒绝受保护查询，但可本地退出；写请求来源校验及限流已实现。
- 添加单实例配置示例和运行说明。真实员工账号、登录页面及生产存储不在本次完成范围。

验证覆盖：鉴权、环境隔离、权限撤销、上游过期、退出后重放、跨源请求、限流、存储重启与加密、配置错误，以及真实 HTTP 适配器的有效/无效身份、超时、重定向和错误响应。

`npm run check` 验证包括：类型检查、17 项测试、构建及启动构建产物的 HTTP 冒烟检查。冒烟检查使用临时本机端口，并在结束后停止其启动的进程。Git 上传结果见当次交付说明。

## 2026-09-07 / F01b 员工登录与授权环境工作台

- 添加 Next.js / React 前端、中文登录页及授权环境工作台，按项目既有 API 的权限边界显示内容。
- 新增服务端密码登录适配，浏览器只接收本平台 HttpOnly 会话；本次新建的上游 refresh session 立即撤销。
- 统一加载、错误、空环境、身份失效和退出反馈，保持环境选择 URL 状态。
- 创建 DESIGN.md、UX-CONTRACT.md、设计令牌生成与漂移检查、前端严格审计配置。
- 完成 22 项后端测试、前后端构建与类型检查及浏览器流程验证；详情见 `docs/ui-verification.md`。
- 当前真实员工联调仍待实际授权配置；本次没有修改 MedicalCareWeb 工程或账号。

## 2026-09-07 / F02 服务目录

- 实现服务登记、编辑、启停、环境隔离、幂等创建、版本冲突检测及事务审计。
- 新增部署侧目标白名单，页面仅选择当前环境的已批准目标。
- 服务表单使用共享模态对话框，包含未保存修改保护、启停确认和失败保留输入。
- 验证：26 项后端测试、前后端类型检查与构建、进程冒烟检查通过；浏览器验证创建、编辑、启停、原生下拉键盘选择、409 保留输入、Escape 草稿保护及 390px 对话框布局。Premium strict audit 无违规。
- F01b 及更早提交已在 `a894cbd` 推送成功。F02 提交 `e71ffbc` 已成功推送至 `origin/main`。

## 2026-09-07 / F03a HTTP 健康探测

- 实现固定 IP 白名单 HTTP 探测、自动调度、手动触发、角色与环境授权、3 秒超时、并发及重复采样保护。
- SQLite 持久化采样与审计；区分成功、HTTP 异常、连接失败、超时、中断、无数据和过期，按当前配置的 5 分钟有效样本统计成功率。
- 服务页面增加最近响应头耗时、成功率、采集时间及即时探测；支持 30 秒刷新和客户端过期判定。
- 验证：35 项后端测试、前后端类型检查与构建、进程冒烟检查通过。真实本机 HTTP 测试覆盖固定地址/Host、重定向、超时、响应体不下载；SQLite 重启、窗口边界、恢复、幂等和并发验证通过。
- 浏览器使用隔离身份与本机 `/health/live` 验证即时探测、自动恢复、启停及配置失效；路由注入旧时间戳验证过期未知和隐藏旧指标。桌面和 390px 窄屏截图已检查，无横向溢出。详情见 `docs/ui-verification.md`。
- 本次未使用真实员工账号或生产环境，不将本机探测成功记为 M1 完成。Git 上传结果随本次交付报告记录。
